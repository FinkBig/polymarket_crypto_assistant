services:
  - type: web
    name: polymarket-crypto-assistant
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn src.web.server:app --host 0.0.0.0 --port $PORT"
    plan: free
